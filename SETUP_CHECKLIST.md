# 週次レポートの設定と障害切り分け

この文書は修正候補ブランチ用です。実メール送受信・Claude APIは未検証です。全体の動作保証や作業時間の保証はありません。

## 1. 配布元を確認

https://github.com/rei2342/note-trend-collector

現在の修正案: https://github.com/rei2342/note-trend-collector/pull/1

修正案は本番mainへ未反映です。mainをForkしただけでは修正版になりません。導入目的の購入・導入は修復完了の案内を待ってください。

## 2. 送信設定

GMAIL_ADDRESSに送信元、GMAIL_APP_PASSWORDにGoogleのアプリパスワード、TO_EMAILに受信先を指定します。通常のログインパスワードは使いません。Googleアプリパスワードの利用可否はアカウント設定によります。

## 3. AI分析を使う場合

ANTHROPIC_API_KEYを設定します。AI分析は従量課金です。利用前に残高・モデルの利用可否・支出上限を確認してください。キー未設定やAPIエラーでは集計のみのレポートになります。メールが届いてもAI分析が成功したとは限りません。

## 4. 最初にテスト

`python -m unittest discover -s tests -v` は外部送信しません。
`python main.py` とWeekly Trend Reportは実際の収集・API利用・メール送信を実行します。
宛先と利用費用を確認してから実行してください。

## 5. 成功の確認

- 実行ログが完了している
- note/はてブの取得件数が想定範囲にある
- AI分析か集計版かが明示されている
- 出典URLと本文を照合できる
- 指定した受信先に届いている
- その後の定期実行でも届くことを別途確認する

## 障害の切り分け

| 症状 | 確認すること |
| --- | --- |
| Actionsが開始しない | Actionsの有効状態、対象ブランチ、workflowの存在、実行履歴 |
| 依存ライブラリの導入で失敗 | 失敗したステップのログ、Pythonバージョン、requirements |
| 記事が0件 | 収集先の接続・HTTPエラー、タグとカテゴリ、キーワード条件 |
| 集計版になった | APIキーの設定名、利用残高、モデルの利用可否、HTTPエラー、応答の打ち切り |
| SMTP認証エラー | 送信元アカウントとアプリパスワードの対応、Google側の利用可否 |
| 実行は成功したが未着 | TO_EMAIL、迷惑メール、受信制限、実行ログの送信完了 |
| メールは届くが内容が不自然 | 取得件数、原文、欠損値、AIが仮説を事実としていないか |

エラーの原因を決めつけてパスワードを再発行しないでください。最初に失敗ステップを特定します。
問い合わせ時は実行日時・失敗ステップ・機密情報を除いたエラーを共有し、キーやパスワードは掲載しないでください。
